# möbius Gateway 0.9.8

- Reports each session's exact context limit using the effective compaction trigger when enabled and the model context window otherwise.
- Keeps context-limit presentation consistent across terminal and Apple clients.

Gateway protocol 40, configuration 18, and chat specification 9 are required.
