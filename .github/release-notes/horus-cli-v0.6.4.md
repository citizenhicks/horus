## Highlights

- Bundles gateway protocol 19, complete durable-history pagination, and `horus` 0.6.3.

## Install or upgrade

```sh
horus-gateway exit
cargo install --force --locked horus-cli --version 0.6.4
horus-gateway serve --background
```

- Gateway configuration remains version 11 and checkpoint storage remains schema 4.
