# Horus CLI 0.8.2

- Bundles Horus 0.8.2 and Horus Gateway 0.8.2.

Upgrade both installed commands together:

```sh
horus-gateway exit
cargo install --force --locked horus-cli --version 0.8.2
horus-gateway serve --background
```
