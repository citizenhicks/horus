## Highlights

- Bundles `horus` 0.7.9 so authorized commands can modify the complete chat workspace without
  path-specific metadata exceptions.
- Keeps gateway state and credentials outside the workspace and unreadable to commands.

## Upgrade

- Gateway protocol 25 and all persisted data versions are unchanged.
