# möbius Gateway 0.9.6

- Supports `MOBIUS_GATEWAY_SANDBOX_PROC=empty` on restricted nested Linux runtimes while retaining user, PID, and network isolation.
- Rejects unknown proc sandbox modes at configuration time.

Gateway protocol 38, configuration 17, and chat specification 8 are unchanged.
