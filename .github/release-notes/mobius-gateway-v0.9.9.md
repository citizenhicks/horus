# möbius Gateway 0.9.9

- Configures custom reasoning catalogs and hosted web-search policy when registering a provider instance.
- Keeps repeated provider reconciliation from overwriting later user model choices.

Gateway protocol 40, configuration 18, and chat specification 9 are unchanged.
