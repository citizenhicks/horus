# möbius 0.9.2

- Replaces the skills middleware with managed extensions, including lazy skill resources and sandboxed OpenAI-compatible command hooks.
- Adds typed lifecycle controls, tool-owned hook identities, and safer command authorization.
- Simplifies frontend contributions and capability commands.

This 0.x release updates public middleware APIs and requires gateway protocol 37, configuration 17, and chat specification 8.
