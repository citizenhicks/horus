## Highlights

- Bundles gateway protocol 15 with thread-grouped recent runs.

## Install or upgrade

```sh
mobius-gateway exit
cargo install --force --locked mobius-cli --version 0.5.3
mobius-gateway serve --background
```

- Gateway configuration and checkpoint storage remain unchanged from 0.5.2.
