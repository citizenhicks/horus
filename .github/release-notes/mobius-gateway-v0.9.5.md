# möbius Gateway 0.9.5

- Returns user uploads and agent-created files through one session-file listing.
- Preserves existing stored artifacts while removing the separate artifact catalog contract.

Upgrade the gateway and clients together: this release uses protocol 38, configuration 17, and chat specification 8.
