## Highlights

- Bundles `horus` 0.7.2 so native OpenAI HTTP Responses routes request automatic reasoning
  summaries while compatible endpoints remain opt-in.

## Upgrade

- Gateway protocol 23, configuration version 14, chat specification version 6, checkpoint version
  5, and SQLite schema 4 are unchanged.
