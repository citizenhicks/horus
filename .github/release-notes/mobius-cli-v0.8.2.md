# möbius CLI 0.8.2

- Bundles möbius 0.8.2 and möbius Gateway 0.8.2.

Upgrade both installed commands together:

```sh
mobius-gateway exit
cargo install --force --locked mobius-cli --version 0.8.2
mobius-gateway serve --background
```
