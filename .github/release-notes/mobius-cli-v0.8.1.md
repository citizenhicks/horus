# möbius CLI 0.8.1

- Bundles möbius 0.8.1 and möbius Gateway 0.8.1.

Upgrade both installed commands together:

```sh
mobius-gateway exit
cargo install --force --locked mobius-cli --version 0.8.1
mobius-gateway serve --background
```
