## Highlights

- Bundles `horus` 0.6.11 so compaction markers remain visible in transcript history after
  reconnects, gateway restarts, and paginated history loads.

## Upgrade

- Protocol 22, gateway configuration version 13, chat specification version 6, checkpoint version
  5, and SQLite schema 4 are unchanged.
