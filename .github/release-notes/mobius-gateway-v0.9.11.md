# möbius Gateway 0.9.11

- Migrates Gateway configuration 18 to 19 atomically while preserving installed extensions and other host state.
- Treats the MCP-server list as empty for extensions installed before authenticated MCP support.

Gateway protocol 44, configuration 19, and chat specification 9 are unchanged.
