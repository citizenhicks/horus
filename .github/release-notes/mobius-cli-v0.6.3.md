## Highlights

- Bundles gateway protocol 18 and the corrected workspace file catalog.

## Install or upgrade

```sh
mobius-gateway exit
cargo install --force --locked mobius-cli --version 0.6.3
mobius-gateway serve --background
```

- Gateway configuration remains version 11 and checkpoint storage remains schema 4.
