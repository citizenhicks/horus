# möbius 0.9.4

- Adds an opt-in empty `/proc` mount for Linux hosts that permit user and PID namespaces but forbid nested procfs mounts.
- Keeps the default private procfs sandbox unchanged.

Gateway protocol 38, configuration 17, and chat specification 8 are unchanged.
