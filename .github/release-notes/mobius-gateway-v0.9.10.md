# möbius Gateway 0.9.10

- Publishes the first custom reasoning effort as the provider default, so clients select the same effort the runtime executes.

Gateway protocol 40, configuration 18, and chat specification 9 are unchanged.
