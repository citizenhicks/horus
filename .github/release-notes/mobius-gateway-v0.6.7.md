## Highlights

- Advertises the official provider identity symbols for OpenAI API-key and Kimi routes.
- Bundles `mobius` 0.6.6.

## Upgrade

- Gateway protocol 21, configuration version 12, checkpoint JSON version 5, and SQLite schema 4
  are unchanged. Existing clients continue to decode the extensible symbol field.
