# möbius 0.9.6

- Exposes the effective compaction trigger after reserved response capacity so companion frontends can report context fill against the runtime boundary.

Gateway protocol 40, configuration 18, and chat specification 9 are supported by the companion Gateway release.
