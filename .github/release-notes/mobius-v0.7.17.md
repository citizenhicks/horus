# möbius 0.7.17

- Clarifies that Full Access shell commands have host filesystem and network access.
- Documents that gateway state, TLS credentials, and stored provider credentials may be accessible to Full Access shell commands.
- Keeps file tools workspace-scoped and preserves credential environment-variable scrubbing.
