## Highlights

- Bundles gateway protocol 19, complete durable-history pagination, and `mobius` 0.6.3.

## Install or upgrade

```sh
mobius-gateway exit
cargo install --force --locked mobius-cli --version 0.6.4
mobius-gateway serve --background
```

- Gateway configuration remains version 11 and checkpoint storage remains schema 4.
